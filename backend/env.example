# Configuration de la base de données PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=electro_db

# URL de connexion Prisma
# Remplacez les valeurs ci-dessous par vos propres identifiants PostgreSQL
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/electro_db?schema=public"

# Configuration de l'application
PORT=3000
NODE_ENV=development

# Configuration JWT
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=24h

