# Dockerfile pour le développement du frontend Next.js

FROM node:20-alpine

WORKDIR /app

# Installer les dépendances
COPY package*.json ./
RUN npm install

# Copier le code source
COPY . .

EXPOSE 3000

ENV NODE_ENV=development
ENV NEXT_TELEMETRY_DISABLED=1

# Démarrer en mode développement avec hot-reload
CMD ["npm", "run", "dev"]

